
[Params]
  [Params.Terminal]
  # Note: color schema
  # Note: You can choose between
  # Note: ["monokai", "powershell", "gruvbox_light", "gruvbox_dark", "solarized_light", "solarized_dark", "ubuntu", "retro"]
  schema = "solarized_dark"

  # Note: in terminal
  # [userName]@[pcName]:~/$ cd [workDir]
  # [userName]@[pcName]:~/[workDir]$ cat [profile]
  #
  # [description]
  #
  # Note: if you set Params.Tree > user = true
  # [userName]@[pcName]:~/[workDir]$ tree ./[folderName]/
  # ./[folderName]/
  # ...
  # Note: result of the tree command
  userName = "gsb"
  pcName = "labs"
  workDir = "technology"
  profile = "about.txt"

  # Note: speed at which text is displayed on the terminal
  terminalDelay = 20

  # Note: speed at which text is displayed on the activity pages
  activityDelay = 5

  description = """
  
  
  Blockchain intelligence and crypto analytics.


  """
  [Params.Tree]
  use = true
  folderName = "learn_more"
  # Note: ["ACTIVITY", "URL or PATH TO YOUR MARKDOWN FILE"]
  files = [ 
    ["cyee", "https://www.christopheryee.org/"],
    ["ngadacz", "https://www.linkedin.com/in/nicholasgadacz/"]
  ]
  

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true



